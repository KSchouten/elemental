#' The Auth interface class that any authentication module should subclass in order to work with the Elemental framework
#' 
#' Subclass it to implement a specific authentication technology or SSO etc. for your dashboard
#' 
#' @export
ElementalAuth <- R6::R6Class(
  "ElementalAuth", 
  
  private = list(
    
  ),
  
  public = list(
    
  )
)

